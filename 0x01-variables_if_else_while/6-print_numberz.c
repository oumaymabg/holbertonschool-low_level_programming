#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int char;

for (char = '0'; char <= '9'; char++)
{
putchar(ch);
}
putchar('\n');
return (0);
}
