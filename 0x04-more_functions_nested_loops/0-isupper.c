#include "holberton.h"
/**
* _isupper - a function that checks for uppercase charactery
* @c: integer
* Return: Always 0.
*/
int _isupper(int c)
{
while (c >= 'A' && c <= 'Z')
return (1);
return (0);
}
